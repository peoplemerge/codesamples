package com.peoplemerge.cohesion.coincidental;

public interface CanDriveCab {

	/** 
	 * Returns directions from your currento place to the one you specify 
	 * 
	 * @param to
	 * @return
	 */
	String direct(String to);
}
